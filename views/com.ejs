<!Doctype html>
<html>
<head>
  <meta charset="utf-8">
  <title>Show message</title>
</head>
<body>
  <h2>会社名</h2>
  <table>
      <% for( let row of data ) { %>
        <tr>
          <td>
          <a href="/com/"<%= row.id %>><%= row.id %></a>
            <td><%= row.name %></td>
        </tr>
      <% } %>
  </table>
</body>
</html>